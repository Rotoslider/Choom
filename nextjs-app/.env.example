# Choom AI Companion - Environment Configuration
# Copy this to .env and fill in your values

# ─── LLM ────────────────────────────────────────────────────
# LM Studio, Ollama, or any OpenAI-compatible endpoint
LLM_ENDPOINT=http://localhost:1234/v1
LLM_MODEL=local-model

# Vision-capable LLM (can be same server as LLM)
VISION_ENDPOINT=http://localhost:1234

# ─── Services ───────────────────────────────────────────────
MEMORY_ENDPOINT=http://localhost:8100
TTS_ENDPOINT=http://localhost:8004
STT_ENDPOINT=http://localhost:5000
IMAGE_GEN_ENDPOINT=http://localhost:7860

# ─── Weather ────────────────────────────────────────────────
# Sign up at https://openweathermap.org/api (free tier)
OPENWEATHER_API_KEY=
DEFAULT_WEATHER_LOCATION=Your City, ST
DEFAULT_WEATHER_LAT=0.0
DEFAULT_WEATHER_LON=0.0

# ─── Search ─────────────────────────────────────────────────
# Sign up at https://brave.com/search/api/ (free tier: 2000 queries/month)
BRAVE_API_KEY=
# Or self-hosted SearXNG:
# SEARXNG_ENDPOINT=http://localhost:8888

# ─── Workspace ──────────────────────────────────────────────
# Where Choom stores project files (default: ~/choom-projects)
# WORKSPACE_ROOT=/home/youruser/choom-projects

# ─── Owner ──────────────────────────────────────────────────
# Your name (used in morning briefings)
OWNER_NAME=friend
