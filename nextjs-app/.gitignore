*.bak

# Runtime data (backups, logs, traces, presence state, yt reports, pending files, etc.)
# Generated at runtime — not source. The tracked data/presence/.gitkeep stays tracked.
/data/

# LAN HTTPS front door (scripts/setup-lan-https.sh). Holds a private key.
/certificates/
